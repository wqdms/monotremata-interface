apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-monotremata.fullname" . }}
  labels:
    {{- include "helm-monotremata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-monotremata.selectorLabels" . | nindent 4 }}
